Weekly self-review. Query the brain for recent activity:
```bash
cd "${CASHEW_HOME}" && KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/cashew_context.py context --hints "recent decisions projects status changes priorities patterns"
```

Review and, if needed, update the project docs directly:
1. **CLAUDE.md** — update if personality/capabilities have evolved
2. Any status/heartbeat file you maintain — check/uncheck items, update stats

Check for:
- Priorities that have shifted
- Patterns noticed in recent brain activity
- Stale information that contradicts the brain

Make the edits directly. Then report what you changed and why. Keep it concise.
